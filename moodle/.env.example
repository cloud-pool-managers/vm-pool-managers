# Copier en moodle/.env (gitignoré) avant `docker compose up -d`.
# Le mot de passe admin doit respecter la politique Moodle :
# >= 8 caractères, au moins 1 majuscule, 1 minuscule, 1 chiffre, 1 caractère spécial.
MOODLE_ADMIN_USER=admin
MOODLE_ADMIN_PASSWORD=Cpm_Moodle_2026
MOODLE_DB_ROOT_PASSWORD=change-me-root
MOODLE_DB_PASSWORD=change-me-moodle
